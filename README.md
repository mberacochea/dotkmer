# dotnet core kmer counter

This is really simple kmer counter built with C# using .NET Core.

It has:

- FastQ parser (a simple parser)
- Kmer counter

# run

dotnet run {k} {fastq} - dotnet run 5 reads.fastq